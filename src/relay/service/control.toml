name = "@metorial/relay"
group = "oss"

[dev]
run = ["dev:start"]

[endpoints.rpc]
port = 52110
env = [{ key = "RELAY_PORT", value = "{{BIND_PORT}}" }]

[[resources]]
type = "redis"
env = [{ key = "REDIS_URL", value = "redis://{{HOSTNAME}}:{{PORT}}/0" }]

[dev.env]
METORIAL_ENV = "development"
METORIAL_SOURCE = "oss"
IS_ENTERPRISE = "false"
METORIAL_HOSTNAME = "{{HOSTNAME}}"
NODE_ENV = "development"

[dev.db.main]
package = "@metorial/relay"
name = "relay"
engine = "postgres"
env = "DATABASE_URL"
