model TenantOAuthCallbackUrl {
  oid BigInt @id
  id  String @unique

  slug String @unique

  tenantOid BigInt
  tenant    Tenant @relation(fields: [tenantOid], references: [oid])

  providerOid BigInt
  provider    Provider @relation(fields: [providerOid], references: [oid])

  providerTypeOid Int
  providerType    ProviderType @relation(fields: [providerTypeOid], references: [oid])

  createdAt DateTime @default(now())

  @@unique([tenantOid, providerOid])
}
