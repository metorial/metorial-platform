{
  "name": "@metorial-mono/shuttle-client",
  "version": "1.0.0",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "src/**",
    "dist/**",
    "README.md",
    "package.json"
  ],
  "author": "Tobias Herber",
  "license": "Apache 2",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "lint": "prettier src/**/*.ts --check",
    "build": "rm -rf ./dist && microbundle && dts-bundle-generator --config dts-bundle-generator.config.json",
    "prepublish": "bun run build"
  },
  "dependencies": {
    "@lowerdeck/rpc-client": "^1.1.0",
    "@metorial/util-websocket": "^1.0.0",
    "@types/bun": "^1.3.7"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "dts-bundle-generator": "^9.5.1",
    "microbundle": "^0.15.1",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}