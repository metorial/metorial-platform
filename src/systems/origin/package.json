{
  "name": "@metorial/origin-root",
  "private": true,
  "scripts": {
    "dev": "docker compose -p origin2 -f ./docker-compose.dev.yml up --pull always --build code-bucket service"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.8.20",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.2.15"
  },
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
