[control]
name = "voyager"

[service]
port = 52060
dockerfile = "src/systems/voyager/service/Dockerfile.generated"
build_context = "oss"
health = { cmd = "bun -e \"fetch('http://localhost:12121/').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\"" }

[build]
builder = "node"
mode = "generated"
context = "oss"
project = "@metorial/voyager"
target = "server:build"
dockerfile = "Dockerfile.generated"

[[build.automations]]
name = "voyager-prisma"
kind = "run-targets"
target = "prisma:generate"
projects = ["@metorial/voyager"]

[build.artifacts]
copy = [
  { from = "dist", to = "/app/dist" },
  { from = "prisma", to = "/app/prisma" },
  { from = "prisma.config.ts", to = "/app/prisma.config.ts" },
  { from = "/app/node_modules", to = "/app/node_modules" }
]

[build.runtime]
base_image = "oven/bun:1.2-alpine"
packages = ["curl"]
env = { NODE_ENV = "production" }
expose = [52060]
command = "bun prisma db push --accept-data-loss && bun /app/dist/index.js"
healthcheck = "curl -f http://localhost:52060/metorial-voyager && curl -f http://localhost:12121/ || exit 1"

[test.unit]
command = "bun run test:unit"

[test.e2e]
command = "bun run test:e2e"
setup = ["bun prisma generate", "bun prisma db push --accept-data-loss"]
cwd = "/app/src/systems/voyager/service"
runner = "service"

[[databases]]
adapter = "postgres"
name = "voyager-test"

[[databases]]
adapter = "postgres"
name = "voyager-search"

[env]
DATABASE_URL = "$db.voyager-test.url"
SEARCH_DATABASE_URL = "$db.voyager-search.url"
REDIS_URL = "redis://$deps.redis.6379/0"

[[deps]]
name = "postgres"
preset = "postgres"

[deps.env]
POSTGRES_USER = "voyager"
POSTGRES_PASSWORD = "voyager"
POSTGRES_DB = "voyager"

[[deps]]
name = "redis"
preset = "redis"
