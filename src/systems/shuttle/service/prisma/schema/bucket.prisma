model ConnectionLogsStorageBucket {
  oid               BigInt             @id
  bucket            String             @unique
  createdAt         DateTime           @default(now())
  serverConnections ServerConnection[]
}
