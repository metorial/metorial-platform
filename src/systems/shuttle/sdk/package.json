{
  "name": "@metorial/root",
  "private": true,
  "scripts": {
    "_test": "bun x nx run-many --target=test --all --parallel=1",
    "_build": "bun x nx run-many --target=build --all"
  },
  "devDependencies": {
    "nx": "^22.7.3",
    "prettier": "^3.5.3",
    "typescript": "5.8.2"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.2.15"
  },
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
