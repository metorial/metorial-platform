{
  "name": "@metorial/root",
  "private": true,
  "scripts": {
    "_test": "turbo run --concurrency=1 --ui=stream test",
    "_build": "turbo run --concurrency=1 --ui=stream build"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.8.20",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.2.15"
  },
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
