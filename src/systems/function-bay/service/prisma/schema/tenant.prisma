model Tenant {
  oid BigInt @id
  id  String @unique

  identifier String @unique
  name       String

  createdAt             DateTime               @default(now())
  runtimeForgeWorkflows RuntimeForgeWorkflow[]
  functions             Function[]
  enclaves              Enclave[]
}
