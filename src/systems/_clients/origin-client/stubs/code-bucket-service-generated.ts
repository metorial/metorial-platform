/**
 * Stub for client package type generation only. The real implementation lives in
 * `origin/apps/code-bucket` and is used at runtime by origin-service.
 */
export declare function createCodeBucketClient(opts: {
  address: string;
  credentials?: unknown;
  options?: Record<string, unknown>;
}): unknown;

export type McpManagerClient = unknown;
