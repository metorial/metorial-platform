export let UNIFIED_ID_PREFIX = 'mt/x/';

export let PING_ID_PREFIX = 'mt/ping/';
export let INIT_ID_PREFIX = 'mt/init/';
export let ONE_OFF_ID_PREFIX = 'mt/1o/';

export let MCP_IDS = {
  PING: PING_ID_PREFIX,
  INIT: INIT_ID_PREFIX,
  ONE_OFF: ONE_OFF_ID_PREFIX,

  UNIFIED: UNIFIED_ID_PREFIX
};
