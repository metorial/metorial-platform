model ChatWorkspace {
  oid BigInt @id
  id  String @unique

  workspaceId String
  name        String?
  domain      String?
  imageUrl    String?

  raw Json

  chatOid BigInt @unique
  chat    Chat   @relation(fields: [chatOid], references: [oid], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  chatChannels ChatChannel[]

  @@unique([workspaceId, chatOid])
}

enum ChatChannelType {
  public
  private
  dm
  group_dm
  shared
  announcement
  forum
  unknown
}

model ChatChannel {
  oid BigInt @id
  id  String @unique

  type         ChatChannelType
  providerType String

  channelId String
  name      String?
  topic     String?
  subject   String?

  memberCount Int?

  context   Json?
  permalink String?

  raw Json

  chatOid BigInt
  chat    Chat   @relation(fields: [chatOid], references: [oid], onDelete: Cascade)

  workspaceOid BigInt?
  workspace    ChatWorkspace? @relation(fields: [workspaceOid], references: [oid], onDelete: Cascade)

  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @default(now()) @updatedAt
  lastInteractionAt DateTime?

  threads  ChatThread[]
  messages ChatMessage[]

  @@unique([chatOid, channelId])
}

enum ChatThreadType {
  conversation
  dm
  post
}

model ChatThread {
  oid BigInt @id
  id  String @unique

  type         ChatThreadType
  providerType String

  threadId String
  subject  String?

  context   Json?
  permalink String?

  rootMessageId String?
  replyCount    Int?
  lastReplyAt   DateTime?

  raw Json

  channelOid BigInt
  channel    ChatChannel @relation(fields: [channelOid], references: [oid], onDelete: Cascade)

  chatOid BigInt
  chat    Chat   @relation(fields: [chatOid], references: [oid], onDelete: Cascade)

  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @default(now()) @updatedAt
  lastInteractionAt DateTime?

  messages ChatMessage[]

  @@unique([channelOid, threadId])
}
