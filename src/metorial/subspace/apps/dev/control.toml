# Disabled for now — use the individual apps instead:
#   @metorial-subspace/app-public (52071)
#   @metorial-subspace/app-worker
# The monolith hits a circular init in integration lifecycle queues.
name = "@metorial/subspace-dev"
group = "oss"
mode = "oss"
