model GlobalLease {
  name String @id

  token     String?
  expiresAt DateTime?

  holderOid Int?
  holder    Cell? @relation(fields: [holderOid], references: [oid], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt
}
