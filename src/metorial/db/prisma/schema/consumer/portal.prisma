enum PortalStatus {
  active
  archived
  deleted
}

model Portal {
  oid BigInt @id @default(autoincrement())
  id  String @unique

  status PortalStatus

  name        String
  description String?
  slug        String  @unique

  /// [PortalAllowedRedirectUrlFilters]
  allowedRedirectUrlFilters Json?

  isDefaultPortal Boolean @default(false)

  organizationOid BigInt
  organization    Organization @relation(fields: [organizationOid], references: [oid], onDelete: Cascade)

  surfaceOid BigInt          @unique
  surface    ConsumerSurface @relation("PortalSurface", fields: [surfaceOid], references: [oid], onDelete: Cascade)

  instanceOid BigInt
  instance    Instance @relation(fields: [instanceOid], references: [oid], onDelete: Cascade)

  workspace Workspace?

  portalSpecificNamespaceOid BigInt?
  portalSpecificNamespace    Namespace? @relation("PortalSpecificNamespace", fields: [portalSpecificNamespaceOid], references: [oid], onDelete: SetNull)

  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @default(now()) @updatedAt
  archivedAt DateTime?
  deletedAt  DateTime?

  namespaceProperties NamespaceProperty[]
}
