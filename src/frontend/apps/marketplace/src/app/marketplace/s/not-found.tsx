'use client';

import { ErrorPage } from '@metorial/pages';

export default () => {
  return (
    <ErrorPage
      title="Server Not Found"
      description="The server you are looking for does not exist."
    />
  );
};
