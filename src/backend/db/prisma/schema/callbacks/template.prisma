model CallbackTemplate {
  oid BigInt @id @default(autoincrement())
  id  String @unique

  eventType CallbackType

  createdAt DateTime @default(now())

  lambdaServerInstances LambdaServerInstance[]
}
