enum ConsumerAccessType {
  server_deployment_template
}

model ConsumerAccess {
  oid BigInt @id @default(autoincrement())
  id  String @unique

  type ConsumerAccessType

  surfaceOid BigInt
  surface    ConsumerSurface @relation(fields: [surfaceOid], references: [oid])

  consumerGroupOid BigInt
  consumerGroup    ConsumerGroup @relation(fields: [consumerGroupOid], references: [oid])

  serverDeploymentTemplateOid BigInt?
  serverDeploymentTemplate    ServerDeploymentTemplate? @relation(fields: [serverDeploymentTemplateOid], references: [oid])

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  @@unique([consumerGroupOid, serverDeploymentTemplateOid])
}
