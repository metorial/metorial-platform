model CellSkillPlugin {
  id String @id

  status SkillPluginStatus

  name String?
  slug String?

  isOwnedByDeployment Boolean

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  @@index([slug])
}
