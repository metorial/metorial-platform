{
  "name": "@metorial/oss",
  "private": true,
  "scripts": {
    "build": "turbo run --ui=stream build",
    "generate:client": "turbo run generate:client && turbo run build",
    "client:generate": "bun run generate:client",
    "client:generate:dashboard": "turbo run generate:client:dashboard",
    "client:generate:consumer": "turbo run generate:client:consumer",
    "dev": "turbo run --concurrency 99 dev",
    "test": "bun ./scripts/test-tools/src/runMonorepoTests.ts",
    "prisma:generate": "sh -c 'cd ./src/backend/db && bun run prisma:generate && cd ../../systems/signal/service && bun run prisma:generate && cd ../../slates/apps/hub && bun run prisma:generate && cd ../../../subspace/db && bun run prisma:generate'",
    "prisma:push": "sh -c 'cd ./src/backend/db && bun run prisma:push && cd ../../systems/signal/service && bun run prisma:push && cd ../../slates/apps/hub && bun run prisma:push && cd ../../../subspace/db && bun run prisma:push'",
    "prepare": "echo prepare",
    "postinstall": "echo postinstall && [ -f \"./vendor/mcp-inspector\" ] && { cd ./vendor/mcp-inspector && npm install; } || { echo \"No vendor dependencies to install.\"; }",
    "preinstall": "[ -f \"../metorial_enterprise_install.sh\" ] && { ../metorial_enterprise_install.sh; } || { [ ! -f \"../metorial_enterprise_install.sh\" ] && echo \"Ignoring enterprise dependencies.\"; }"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.4",
    "typescript": "5.8.2"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.2.15"
  },
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "_clients/metorial-node/*/*",
    "clients/metorial-dashboard",
    "clients/metorial-consumer",
    "scripts/*",
    "vendor-placeholder/*",
    "src/backend/*",
    "src/backend/*/*",
    "src/backend/*/*/*",
    "src/frontend/*",
    "src/frontend/*/*",
    "src/frontend/*/*/*",
    "src/packages/*",
    "src/packages/*/*",
    "src/packages/*/*/*",
    "src/systems/*/*",
    "src/systems/*/*/*",
    "src/systems/*/*/*/*",
    "src/systems/*/*/*/*/*"
  ],
  "dependencies": {
    "sade": "^1.8.1",
    "ts-proto": "^2.7.5"
  }
}